{
  "name": "snail-runner",
  "version": "0.0.1",
  "description": "Snail runs a lot of shell commands in parallel and shows the output for each of them",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "snail",
    "commands",
    "parallel",
    "shell",
    "async"
  ],
  "author": "Karuppiah",
  "license": "ISC",
  "dependencies": {
    "aigle": "^1.12.0",
    "chalk": "^2.4.1",
    "shelljs": "^0.8.2"
  },
  "devDependencies": {
    "eslint": "^5.3.0",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-node": "^7.0.1",
    "eslint-plugin-promise": "^3.8.0",
    "eslint-plugin-standard": "^3.1.0"
  }
}
